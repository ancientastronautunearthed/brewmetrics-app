// WARNING: CONTAINS TEMPORARY INSECURE RULE FOR EMULATOR TESTING ONLY
// MUST BE REVERTED AND REPLACED WITH SECURE CLOUD FUNCTION APPROACH BEFORE DEPLOYMENT

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Rule for brewery-specific data (e.g., user profiles - assuming this exists)
    // Keep this rule as it was - secure for brewer's own data
    match /breweries/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Rule for batches
    // Keep these rules as they were
    match /batches/{batchId} {
      allow read: if true; // Anyone can read batch details
      allow create: if request.auth != null && request.resource.data.breweryId == request.auth.uid; // Brewer can create
      allow update, delete: if request.auth != null && resource.data.breweryId == request.auth.uid; // Brewer can update/delete OWN batches (using existing resource data)
    }

    // Rule for feedback
    match /feedback/{feedbackId} {
      allow create: if true; // Anyone can submit feedback

      // --- THIS IS THE MODIFIED LINE ---
      // TEMPORARY INSECURE RULE FOR EMULATOR TESTING: Allows any read operation.
      allow read: if true;
      // --- END OF MODIFIED LINE ---

      // Prevent updates or deletes by client-side code
      allow update, delete: if false;
    }

    // Add any other collection rules here...

  }
}